export default class Persons {
    constructor(pid, firstName, lastName, age) {
        this.pid = pid;
        this.firstName = firstName;
        this.lastName = lastName;
        this.age = age;
    }
}